// The dashboard is an ES module entrypoint. Keeping the runtime in its own
// module makes Vite's output deterministic while preserving the existing
// browser-facing behavior and embedded asset URL.
import './dashboard.js';
